Neata Adrian 321CB

	Pentru tema la PC am impartit fiecare task intr-o functie separata care se conecteaza la server si trimite si primeste mesaje. Pentru partea de compunere a cererilor de tip POST si GET m-am folosit de codul scris la laboratorul de HTTP in care am mai modificat functiile compute_post_request si compute_get_request pentru a contine toate campurile necesare.
	Nu am extras campul type din raspunsurile serverului deoarece mi s-a parunt irelevant. Pentru formatul JSON am ales biblioteca Parson.c deoarece a fost sugerata in enuntul temei si pe forum si mi s-a parut la prima vedere si usor de folosit, lucru care s-a dovedit a fi adevarat dupa ce m-am apucat de tema. Pentru extragerea informatiei am pus intr-un JSON_Value intreg continutul si apoi l-am parsat in obiecte de tip JSON_Object, ca mai apoi sa apelez functia json_object_get_string pentru a scoate informatia de care am nevoie. Am mai folosit si unde s-a patut o parcurgere a tuturor cheilor si valorilor unui JSON_Object (in functia getCredentials).
	Task1: am compus o cerere de tip GET pentru adresa ip a server-ului si url-ul dat din enunt pe care am trimis-o, iar mai apoi am asteptat un raspuns de la server.
	Task2: am extras din raspunsul server-ului de la Task1 url-ul, tipul continutului si datele aferente mesajului cu ajutorul functiilor implementate in biblioteca Parson.c . De asemenea am mai extras din raspuns si cookie-urile sale prin intermediul functiei getCookies care extrage din raspunsul serverului toate campurile care incep cu Cookie si le concateneaza intr-un char[] pe toate. Avand toate aceste informatii, am compus cererea POST si am trimis-o server-ului ca mai apoi sa primesc un raspuns de la el.
	Task3: am extras din raspunsul server-ului de la Task2 url-ul si datele aferente mesajului cu ajutorul functiilor implementate in biblioteca Parson.c . Am adaugat un nou camp in cereri cu Authorization: Basic in care introduc codul autorizatiei primit si apoi am format adresa url raspunzand la cele doua ghicitori (getCredentialsTask3). La sfarsit trimit mesajul compus si astept primirea unui raspuns.
	Task4: pentru acest task am trimis o cerere de tip get cu autorizatia si cookie-ul obtinut la Task3.
	Task5: mai intai mi-am creat o noua functie (get_ip) care returneaza prin efect lateral ip-ul  de tip IPV4 a unui hostname dat, iar apoi am apelat-o pentru a afla IP-ul site-ului de vreme, iar apoi m-am conectat la server-ul site-ului facand querry pentru datele cerute in enunt. Dupa aceea am inchis conexiunea, am extras continutul raspunsului server-ului de vreme si l-am retrimis printr-o cerere de tip POST server-ului din tema.
